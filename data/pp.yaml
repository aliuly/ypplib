#
# Try pre-processor features
#
#define ABC 123

x$<ABC>: 433

balh:
 #include ssh/port443.yaml
cheh:
- content: |
     #include --raw vars.bat
- yaho: {}
- sed -i~ -e 's/^127\.0\.0\.1\s*.*$/127.0.0.1 localhost '$$(uname -n)'/' /etc/hosts

cmd_output: |
  #exec echo one two three
run_commands:
  #include sslh.yaml
oh: $<ABC>

#ifdef ABC
yeah: ABC is defined
#ifdef HOME
yeayyea: HOME is defined
#endif
#else
yeah: ABC is NOT defined
#endif
